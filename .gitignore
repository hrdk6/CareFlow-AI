# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Local runtime state
backend/.pgdata*/
backend/.models/
backend/storage/
*.log

# Secrets
.env
backend/.env
frontend/.env.local

# Raw datasets are downloaded by ml/data/download.py (not redistributed here)
ml/data/raw/

# Node
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/next-env.d.ts
frontend/tsconfig.tsbuildinfo

# editor / tooling config (local only)
.idea/
.claude/

# impeccable working files (captures, decision logs, caches)
.impeccable/review/
.impeccable/questions/
.impeccable/hook.cache.json
.impeccable/config.local.json

# Build output (a node_modules outside frontend/ got committed once by accident)
node_modules/
.ppt-build/

# Submission artifacts: the presentation and synopsis live here locally, not in the codebase
.codex-finalizer/
deliverables/
docs/*.pptx
docs/*.pdf
